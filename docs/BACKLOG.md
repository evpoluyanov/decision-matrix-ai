# Бэклог доработок Decision Matrix AI

Обновлён: 19.09.2026. Это очередь следующих изменений и история реализованных задач.

## Как ведём бэклог

- Новые замечания владельца записываем сюда с постоянным ID, датой и критерием приёмки.
- Просьба «добавь в бэклог» или «в следующей доработке» означает только фиксацию: код сейчас не меняем.
- Перед очередным согласованным пакетом сверяем бэклог с фактическим кодом, показываем состав пакета и выполняем выбранные пункты.
- Статусы: **В очереди → В работе → Реализовано → Проверено в Preview**. Выполненные пункты сохраняем в истории.
- Документацию бэклога можно коммитить в рабочую ветку. Слияние в main, Production deployment и миграции боевой базы требуют отдельного разрешения.

## В очереди

| ID | Добавлено | Доработка | Критерий приёмки | Статус |
| --- | --- | --- | --- | --- |
| DM-001 | 03.09.2026 | Сделать кнопку «Отчёт» синей, заметной и выровнять справа. | Кнопка остаётся после раздела «Риски выбранной альтернативы», но справа, с синим фоном и контрастным текстом. Нет наложений на мобильном экране; виден клавиатурный фокус. Отчёт доступен и без успешного анализа рисков. | В очереди |
| DM-002 | 03.09.2026 | Сохранять рабочую позицию после отправки оценки в отчёте. | После ответа на «Насколько Decision Matrix AI помог вам принять решение?» страница не уходит в начало. При POST → redirect → GET остаётся тот же рабочий участок; после исчезновения формы допустим перевод фокуса к благодарности рядом с ней. Если страница стала короче, позиция корректно ограничивается её высотой. Нет переноса прокрутки в другой проект и нарушения «Назад/Вперёд». Сохранённый ответ по-прежнему исключает повторный показ вопроса этому пользователю. | В очереди |
| DM-003 | 03.09.2026 | Уточнить положение и оформление отметки «Текущий выбор» в карточках тарифов. | Во всех трёх карточках отметка находится справа на одной горизонтальной линии с кнопкой, а не ниже неё. Рамка компактная, с небольшими отступами от текста и более закруглёнными углами; отметка не выглядит второй кнопкой и не интерактивна. При смене выбора кнопки не сдвигаются. На узких экранах нет наложений; расположение адаптивно без возвращения прежнего нижнего отступа на широком экране. | В очереди |
| LEGAL-001 | 09.09.2026 | Реализовать автоматическое переподтверждение актуальных юридических документов для уже зарегистрированных пользователей. | Пользователь, у которого отсутствует или устарела версия согласия с `/terms`, `/privacy` и `/consent`, при следующем входе получает понятный экран переподтверждения до доступа к защищённым функциям. Система не принимает согласие молча: пользователь явно подтверждает документы, ссылки доступны, в БД сохраняются новые дата/время и версия для соглашения и согласия на обработку ПД. Пользователь с уже актуальными версиями не получает повторный запрос. Отказ/закрытие не обходят проверку и не приводят к частичной записи; доступен выход из аккаунта. Обновление версии документов повторно запускает этот сценарий. Покрыть тестами старого пользователя, актуального пользователя, отказ, повторную отправку/обновление страницы и сохранение версии без пользовательских текстов в логах. | Реализовано |
| LEGAL-002 | 18.09.2026 | Добавить в админку управление версиями юридических документов. | Для `/terms`, `/privacy` и `/consent` администратор создаёт и редактирует Markdown-черновик, видит безопасный предпросмотр, публикует версию отдельным подтверждением и открывает неизменяемую историю. После публикации новая версия доступна публично и запускает LEGAL-001. Новые таблицы начинаются пустыми: по решению владельца от 18.09.2026 старые тексты и согласия не переносятся; до первой публикации видны статические страницы, а регистрация закрыта до публикации всех трёх документов. | Реализовано |
| INFRA-001 | 19.09.2026 | Зафиксировать и автоматически проверять единственную разрешённую Production-конфигурацию: Amvera PostgreSQL, Yandex Cloud Postbox и MWS. | В Amvera документально сверены имена провайдеров и хосты без раскрытия секретов: `DATABASE_URL` ведёт в актуальную БД Amvera, SMTP — на `postbox.cloud.yandex.net`, LLM — в MWS. Приложение при запуске явно отклоняет отсутствующий или неподдерживаемый Production-провайдер вместо скрытого переключения на прежний сервис. В Amvera отсутствуют неиспользуемые `BREVO_*`, `VERCEL*` и старые Supabase URL/ключи. Проверка не выводит пароли, токены и полные строки подключения. | В очереди |
| INFRA-002 | 19.09.2026 | Полностью удалить Brevo из исполняемого кода, конфигурации и актуальных тестов. | Отправка писем поддерживает только SMTP/Yandex Cloud Postbox; удалены Brevo API URL, функция `_send_via_brevo`, выбор `EMAIL_PROVIDER=brevo`, переменные `BREVO_*`, Brevo-сценарии и тексты тестового письма. При неполной SMTP-конфигурации сервис закрывается с безопасной ошибкой и не пытается обратиться к другому провайдеру. Сценарии регистрации, повторной отправки и восстановления доступа покрыты тестами; поиск `brevo|sendinblue|notify.dmatrix.tech` не находит исполняемых или актуальных конфигурационных ссылок, исторические материалы при необходимости явно помечены архивными. | В очереди |
| INFRA-003 | 19.09.2026 | Вывести Brevo из внешней инфраструктуры после подтверждения Yandex Postbox. | После контрольной доставки регистрационного и восстановительного письма через Yandex Postbox отозван Brevo API-ключ, удалены отправитель/домен в Brevo и делегирование `notify.dmatrix.tech` на `ns1.sendinblue.com`/`ns2.sendinblue.com` в REG.RU. После распространения DNS публичная проверка не показывает Sendinblue/Brevo; в журнале Brevo после даты переключения нет новых обращений. Внешние удаления выполняются только после отдельного подтверждения владельца. | В очереди |
| INFRA-004 | 19.09.2026 | Полностью вывести Vercel из проекта и заменить Vercel-специфичную логику на проверенную конфигурацию Amvera. | До удаления `vercel.json` отключены Git-интеграция и Production deployment в Vercel, удалены привязки доменов и остановленный проект `decision-matrix-ai`; публикация ветки или `main` больше не может создать Vercel deployment. Из кода и актуальных тестов удалены `VERCEL`, `VERCEL_ENV`, `x-vercel-forwarded-for` и тексты интерфейса про Vercel. Secure-cookie, CSRF, запрет индексации Preview и блокировка платного ИИ управляются нейтральными настройками окружения. На Amvera отдельно подтверждён достоверный адрес клиента за доверенным прокси, поэтому лимиты не объединяют всех пользователей по внутреннему адресу прокси и не доверяют поддельному `X-Forwarded-For`. `dmatrix.tech` и `www.dmatrix.tech` продолжают отвечать только через Amvera. Внешнее удаление проекта Vercel выполняется только после отдельного подтверждения владельца. | В очереди |
| INFRA-005 | 19.09.2026 | Завершить вывод Supabase и убрать устаревшие активные инструкции. | Подтверждено, что актуальные `DATABASE_URL` и `MIGRATION_DATABASE_URL` не ведут в Supabase. `scripts/check_database_schema.py` использует нейтральные формулировки PostgreSQL, текущие инструкции описывают Amvera, а старые отчёты о Vercel/Supabase/Brevo явно обозначены историческими. После проверки резервной копии старый проект/ключи Supabase удаляются только по отдельному подтверждению владельца; в исполняемом коде и актуальной конфигурации ссылок Supabase нет. | В очереди |
| PRIVACY-001 | 19.09.2026 | Убрать обязательные обращения браузера к зарубежному CDN jsDelivr. | Bootstrap 5.3.7 и его лицензия размещены среди локальных статических файлов, шаблоны не обращаются к `cdn.jsdelivr.net`. В чистом браузерном сеансе страницы не создают внешних запросов, кроме явно разрешённых российских сервисов; Яндекс Метрика загружается только после согласия. Добавлена проверка, запрещающая новые непредусмотренные внешние URL в шаблонах. | В очереди |

## Проверка инфраструктуры 19.09.2026

- `dmatrix.tech` и `www.dmatrix.tech` указывают на `158.160.116.199`; TXT `evpoluyanov-dmatrix-app` и ответы приложения подтверждают маршрут через Amvera. У Production-ответов нет заголовков Vercel.
- `https://decision-matrix-ai.vercel.app/health` отвечает `503 DEPLOYMENT_PAUSED`: Vercel не обслуживает Production, но проект ещё существует.
- `notify.dmatrix.tech` всё ещё делегирован на `ns1.sendinblue.com` и `ns2.sendinblue.com`.
- В `main` остаются рабочий Brevo-fallback и Vercel-специфичные ветви кода; Yandex Postbox SMTP, `amvera.yml` и единственный LLM-провайдер MWS также присутствуют.
- На каждой странице браузер загружает Bootstrap с `cdn.jsdelivr.net`; Яндекс Метрика подключается отдельно по механизму согласия.
- Значения секретов и реальный хост `DATABASE_URL` из Amvera извне не видны, поэтому их сверка вынесена в INFRA-001. Старый Supabase по истории оставался резервом; его фактическое удаление не подтверждено.

## Что подтвердил владелец в текущем Preview

03.09.2026, локальный синтетический Preview с имитацией ИИ:

- Значения матрицы сохраняются; рабочая прокрутка после её сохранения не сбрасывается.
- Объяснение результата и окно ожидания отработали по сценарию.
- Анализ рисков: успешный ответ, намеренная ошибка повторного запроса и сохранение прежнего результата проверены. Ограничение частоты запросов не скрывает прежние риски.
- Отчёт открывается после ошибки рисков и расположен после соответствующего раздела. Новое пожелание к оформлению кнопки — DM-001.
- После сохранённого ответа вопрос об отчёте больше не показывается; проверка владельцем пройдена. Скачок страницы после отправки — отдельный дефект DM-002.
- Последовательность выбора 99 ₽ → Pro → бесплатно выполнена; в админке подтверждено: 99 ₽ — 0, Pro — 0, бесплатно — 1, конверсия — 0% / 0% / 100%. Оформление отметки уточнено в DM-003.
- Нули ИИ-статистики на последнем скриншоте объяснены перезапуском синтетического Preview с новой временной базой; дефект учёта по этому скриншоту не зафиксирован.
- Favicon видна во вкладке браузера. Доступность всех четырёх файлов, мобильный вариант и тёмная тема отдельно владельцем не подтверждены.
- Конфликт двух вкладок проверен: после сохранения 8 в первой вкладке попытка сохранить 3 из устаревшей второй вызвала предупреждение; в базе осталась 8.

Эти проверки не являются реальным прогоном MWS или проверкой Production. Остальные сценарии текущего релиза не помечать пройденными без результата владельца.

## Подготовка выпуска 03.09.2026

Владелец завершил ручную приёмку и разрешил коммит и деплой текущего пакета. DM-001–DM-003 остаются в очереди следующей доработки; разрешение выпуска не означает их реализацию.

Перед слиянием в main требуется подтвердить применение к нужной PostgreSQL-базе миграций `b7e2c4d91a63 → d2f48ab17390 → f4c912ab670e`. В среде помощника нет подключения к этой базе и управления терминалом Codespaces. До подтверждения миграций main и Production не обновлять. Реальный MWS, ручной UTM и остальные непройденные сценарии не считать проверенными только из-за разрешения релиза.

## Связанные материалы

- [Сценарий приёмки Preview](preview-reliability-20260902.md).
- [Аудит предыдущего пакета и незакрытые вопросы](reliability-audit-20260902.md): реальная диагностика MWS, сверка расходов, PostgreSQL/сетевые замеры, оставшиеся ручные проверки и утверждение юридических документов. Они не закрываются добавлением этого бэклога.
- [Общее направление развития продукта](ROADMAP.md).
